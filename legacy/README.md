# Legacy Instructor Notebooks

These are the **original instructor notebooks** kept for reference. They are **not** part of the student labs in `notebooks/`.

| File | Notes |
|---|---|
| `test_RCNN.ipynb` | Early R-CNN exploration. Uses a **webcam** (`cv2.VideoCapture(0)`) and `cv2.imshow`, so it will not run on Google Colab. |
| `test_RCNN.html` | HTML export of the notebook above. |
| `test_RCNN.pdf` | PDF export of the notebook above. |

For the maintained, Colab-ready version, see `notebooks/09_object_detection_rcnn/`.
